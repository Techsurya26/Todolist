
A simple and attractive To-Do List web application built using **Flask**, **MySQL**, and **Bootstrap**.  
You can add tasks, mark them as complete, and delete them.  
All tasks are stored permanently inside the database.
